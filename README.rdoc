== README
INSTALLATION INSTRUCTIONS:
INSTALL RUBY

INSTALL GEMS 
  bundle

START RAILS SERVER 
  rails s -p 8080 #loads the single page app with all the assets compiled. 

Open chrome with web security disabled to overcome same origin protection failures during testing
  'open -a Google\ Chrome --args --disable-web-security'

